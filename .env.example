# Optional. LocalForge does not require cloud credentials for its core local path.
# Set only the MCP credentials you want to enable. Keep real secrets out of committed config files.

GITHUB_PERSONAL_ACCESS_TOKEN=
SUPABASE_ACCESS_TOKEN=
BRAVE_API_KEY=
NEON_API_KEY=
FIRECRAWL_API_KEY=
